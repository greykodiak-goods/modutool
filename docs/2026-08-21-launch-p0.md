# 2026-08-21 상업 출시 P0 게이트 — 실행 기록

감사(같은 날)에서 확인된 출시 차단점 4건을 닫고, 전 테스트를 CI에 편입했다.
관련 커밋: `security:` `privacy:` `pricing:` `ci:` (launch-p0 브랜치, 이 문서와 같은 머지).

## 닫은 것 (P0)

| # | 차단점 | 조치 | 검증 |
|---|---|---|---|
| 1 | @auth/core critical 취약점 | 0.41.3 패치 | `npm audit --omit=dev` 0건 |
| 2 | 관리자 권한상승(이메일 선점 가입) | 등재 + 검증된 인증수단(구글 OAuth) 동시 요구, 게이트 3곳 공용화 | 단위 테스트 10단언 + tsc |
| 3 | 방침·실제 불일치(Supabase 표기, 파기 미집행) | Convex로 정정(영·한) + 일일 파기 cron 신설 | 단위 테스트 8단언, 배포 후 crons 확인 |
| 4 | 허위 상품 약속(19개 도구, 확정가) | 실측 문구 + 예정가 명시, CTA 수요계측 추가 | 구조·기능 테스트 통과 |
| 5 | 테스트 수동 실행(CI 부재) | ci.yml — push/PR마다 단위→타입→래칫→감사→빌드→브라우저 19스위트 | CI run 그린 |

덤으로 잡힌 실버그: 윈도 체크아웃(CRLF)에서 build.mjs 허브 필터가 무력화되어
서브사이트 허브에 타 브랜드 섹션·죽은 링크가 생기는 문제(`\r?\n` 허용으로 수정).
구식 테스트 3종(smoke-pdf-merge·subsites-smoke·telemetry.spec)을 현행 UI/백엔드에 맞게 갱신.

## 로컬 실측 결과 (2026-08-21 밤, Windows + Chrome 실브라우저)

- 단위: admin-gate 10단언, retention 8단언 통과
- 타입체크: `tsc --noEmit -p convex` 0오류
- 커버리지 래칫: 도구 40 / 기능검증 40 / 미검증 0
- 단일 빌드: structural 47페이지, smoke-pdf-merge(병합 3쪽 실파싱), admin-smoke, pages-smoke, cv-tools-func, redact-func 전부 통과
- 오프라인 번들: 외부 통신 0건 계약 통과
- 우산 빌드(운영 구조): umbrella·subsites·telemetry·convex-switch·auth·contact·pdf·image·calc·audio·video-trim·video2·pwa-offline 전부 통과

## 남은 것 — 대표(T0) 결정·실행 대기

1. **커스텀 도메인 구매** (thisismypdf.com 등) — 결제라 T0. 구매 후:
   Cloudflare Pages 연결(README 표 그대로), `DEPLOY_ORIGIN`만 바꿔 재배포하면 canonical/sitemap 자동 이관.
2. **Convex prod 배포 전환** — 현재 라이브는 dev 배포(superb-echidna-510).
   prod deploy key 발급(Convex 대시보드) + 구글 OAuth 리다이렉트 URI 추가(구글 콘솔)는 계정 주인만 가능.
3. **AdSense/애드핏 신청** — 커스텀 도메인 연결 후에만 가능(무료 서브도메인 불가).

## 운영 미해결 게이트 (자동화 예정)

- live-check 워크플로 스케줄화(현재 수동 dispatch) — 일일 스모크
- 수요검증 대시보드에 waitlist-cta 집계 노출
